// FLR Cost Estimator — site configuration. Copy this file to flr-config.js next to index.html and fill in both values
// from your Supabase project (Project settings → API). Both are meant to be public: the anon key can do nothing on its
// own, because every database function refuses callers without a valid FLR sign-in (see docs/SUPABASE-SETUP.md).
// NEVER put the service_role key here.
window.FLR_CONFIG = {
  supabaseUrl: "https://jzcxucffzbuuhonepjtw.supabase.co",
  supabaseAnonKey: "sb_publishable_B38pIzkqOb3znR13Q1z_9g_oILllfX2",
};
